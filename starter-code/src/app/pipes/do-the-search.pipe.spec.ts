import { DoTheSearchPipe } from './do-the-search.pipe';

describe('DoTheSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DoTheSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
